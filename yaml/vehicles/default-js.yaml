type: default # Configurable charger
name: Generic (Javascript)
sample: |
  title: My electric vehicle # display name for UI
  capacity: 10 # kWh
  charge:
    type: js
    vm: shared
    script: |
        state = { battery: 95, } ;// vehicle SoC in %
        state.battery ;
    cache: 1s
  # ...
