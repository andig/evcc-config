type: default
name: Zero S (No web api - only bluetooth)
sample: |
  ...

# js functions for Zero S SoC
javascript:
- vm: shared
  script: |
    state = {
      chargepower: 0,
      battery: 50, // motorcycle Zero S
    };
    function get() {
      console.log("state:", JSON.stringify(state));
    }
    function set() {
      console.log(param+":", val);
      console.log("state:", JSON.stringify(state));
    }

...

# vehicle definitions
vehicles:
- name: zero
  title: Zero S # display name for UI
  capacity: 10.8 # ZF 7.2 + ZF 3.6kWh
  type: default
  charge:
    type: js
    vm: shared
    script: |
      if (state.chargepower > 0) state.battery++; else state.battery--;
      if (state.battery < 15) state.battery = 15;
      if (state.battery > 100) state.battery = 100;
      state.battery;
  cache: 1s
